// Technically, this project is a C++ static library.
// We never link with the library this project is only to compile HLSL shaders,
// however the IDE complains when a static library doesn't have any output at all.
void fnComputeShaders()
{
}